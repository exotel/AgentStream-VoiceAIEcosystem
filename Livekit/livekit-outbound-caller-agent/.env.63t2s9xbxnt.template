# Copy to .env.local (or merge) for project 63t2s9xbxnt — API keys MUST be from this Cloud project.
# SIP: sip:63t2s9xbxnt.sip.livekit.cloud  →  WebSocket URL below
LIVEKIT_URL=wss://63t2s9xbxnt.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Outbound PSTN/SIP — used by call_phone.py and agent.py
SIP_OUTBOUND_TRUNK_ID=ST_Bkv2NA6XzogA

# Explicit agent dispatch (dispatch_call.py) — must match the agent name your worker registers (WorkerOptions / Cloud deploy), NOT the CA_ dashboard id
LIVEKIT_AGENT_NAME=outbound-caller

# Reference only (not read by scripts): inbound dispatch rule id; outbound API does not use this
# LIVEKIT_DISPATCH_RULE_ID=SDR_23qvcKTG4Azr
# LIVEKIT_CLOUD_AGENT_ID=CA_Dhp2qAHZ28EL
